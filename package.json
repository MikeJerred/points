{
  "name": "points",
  "private": true,
  "scripts": {
    "install": "run-p install:api install:demo",
    "install:api": "cd points-api-server && npm ci",
    "install:demo": "cd points-demo && npm ci"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
